fish_add_path -a -P /usr/local/sbin
fish_add_path -a -P /usr/local/bin
fish_add_path -a -P /usr/sbin
fish_add_path -a -P /usr/bin
fish_add_path -a -P /sbin
fish_add_path -a -P /bin

fish_add_path -p -P $HOME/bin

# homebrew
fish_add_path -p -P /usr/local/bin
fish_add_path -p -P /opt/homebrew/bin

# rust
fish_add_path -p -P $HOME/.cargo/bin

